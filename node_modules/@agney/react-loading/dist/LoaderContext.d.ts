import { ReactElement, ReactNode } from 'react';
interface ContextProps {
    indicator: ReactElement | null;
}
interface ProviderProps extends ContextProps {
    children: ReactNode;
}
export declare const useLoaderContext: () => ContextProps;
export declare const LoaderProvider: ({ indicator, children }: ProviderProps) => JSX.Element;
export {};
